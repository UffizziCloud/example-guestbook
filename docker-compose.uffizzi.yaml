# Uffizzi extension
x-uffizzi:
  ingress:
    service: guestbook
    port: 3000

services:
  redis:
    image: redis:latest
    deploy:
      resources:
        limits:
          memory: 125M

  guestbook:
    build: .
    environment:
      REDIS_HOST: localhost
    deploy:
      resources:
        limits:
          memory: 125M
